import serverApi from "@/api/server.api";

const getDialogs = () => {
    return serverApi.get('dialogs/all')
}

const getMessages = (messageId, countMessages, dialogId) => {
    return serverApi.get('messages/', {params: {messageId, countMessages, dialogId}})
}

export default {getDialogs, getMessages}
